global:
  scrape_interval: 5s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: 'otel-collector'
    static_configs:
      - targets: ['otel_collector:8889']

alerting:
  alertmanagers:
    - static_configs:
        - targets:
            - alertmanager:9093

